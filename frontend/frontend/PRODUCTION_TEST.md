# 🧪 Тестирование Production сборки

## Быстрая проверка после исправлений

### 1. Локальная сборка и тестирование

```bash
cd frontend

# Соберите production версию
npm run build

# Запустите локальный сервер для тестирования
npx serve dist

# Или используйте встроенный preview
npm run preview
```

Откройте `http://localhost:3000` (или другой порт, который покажет serve) и проверьте:

- ✅ Фон растягивается на весь экран
- ✅ Нет неожиданных отступов
- ✅ Все элементы на своих местах
- ✅ Изображение USDT отображается
- ✅ Стили применяются корректно
- ✅ Адаптивность работает на всех размерах экрана

### 2. Сравнение Dev vs Production

**В двух вкладках браузера:**
1. Dev: `npm run dev` → `http://localhost:5173`
2. Production: `npm run preview` → `http://localhost:4173`

**Сравните визуально:**
- Размеры элементов
- Отступы и padding
- Цвета и фон
- Расположение элементов
- Работа на мобильных (DevTools → Responsive)

### 3. Проверка CSS файлов

После `npm run build` проверьте `dist/assets/`:

```bash
# Посмотрите структуру
ls -la dist/assets/

# Должны быть файлы:
# - index-[hash].js
# - index-[hash].css (или в папке css/)
```

Откройте CSS файл и проверьте:
- ✅ Reset стили в начале файла
- ✅ Нет дублирующихся стилей
- ✅ Правильный порядок импортов
- ✅ Все CSS переменные определены

### 4. Проверка в разных браузерах

Протестируйте в:
- ✅ Chrome/Edge (Chromium)
- ✅ Firefox
- ✅ Safari (если доступен)
- ✅ Мобильный браузер (через DevTools)

### 5. Проверка на Vercel

После деплоя на Vercel:

1. Откройте production URL
2. Откройте DevTools → Network
3. Проверьте загрузку CSS файлов
4. Проверьте отсутствие ошибок в консоли
5. Сравните с dev версией

### 6. Чеклист визуальных проверок

- [ ] Фон занимает весь экран
- [ ] Нет горизонтальной прокрутки
- [ ] Контент не уходит за края
- [ ] Все кнопки кликабельны
- [ ] Форма отображается корректно
- [ ] Изображение USDT видно
- [ ] Анимации работают плавно
- [ ] Toast-уведомления появляются
- [ ] На мобильных все адаптивно
- [ ] На desktop layout корректный

---

## Если проблемы остались

### Проверьте консоль браузера:
- Ошибки загрузки ресурсов
- Ошибки CSS
- Предупреждения

### Проверьте Network tab:
- Все CSS файлы загружаются (статус 200)
- Нет 404 для ресурсов
- Правильные пути к файлам

### Проверьте Computed styles в DevTools:
- Применились ли стили
- Нет ли конфликтов
- Правильные значения CSS переменных

---

## Отладка CSS в Production

Если нужно отладить CSS в production:

1. Временно включите sourcemaps в `vite.config.js`:
```javascript
build: {
  sourcemap: true, // Временно для отладки
}
```

2. Пересоберите и проверьте в DevTools → Sources

3. **Важно:** Отключите sourcemaps перед финальным деплоем!

