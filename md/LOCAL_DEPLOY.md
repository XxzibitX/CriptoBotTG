# 🖥️ Локальный деплой Backend на MacBook

## 📋 Что нужно

- ✅ MacBook с macOS
- ✅ Статический IP адрес
- ✅ Доступ к роутеру (для открытия портов)
- ✅ Node.js установлен

---

## 🚀 Пошаговая инструкция

### Шаг 1: Узнайте ваш статический IP

1. **Внешний IP (публичный):**
   - Зайдите на [whatismyip.com](https://whatismyip.com)
   - Или выполните в терминале:
   ```bash
   curl ifconfig.me
   ```
   - Запишите этот IP (например: `123.45.67.89`)

2. **Внутренний IP (локальный):**
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   ```
   - Обычно выглядит как `192.168.x.x` или `10.0.x.x`
   - Запишите этот IP

### Шаг 2: Настройка Backend

1. **Создайте/обновите `backend/.env`:**
   ```env
   # Server Configuration
   PORT=3000
   HOST=0.0.0.0  # Важно! 0.0.0.0 позволяет принимать соединения извне

   # API Configuration
   RAPIRA_API_URL=https://api.rapira.net/open/market/rates

   # Data Storage
   ORDERS_FILE=./data/orders.json

   # Telegram Bot Configuration
   TELEGRAM_BOT_TOKEN=ваш_токен
   TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id
   ```

2. **Убедитесь, что backend слушает на 0.0.0.0:**
   - В `proxy.js` уже должно быть:
   ```javascript
   const HOST = process.env.HOST || 'localhost'
   ```
   - Если указан `localhost`, измените на `0.0.0.0` или используйте переменную окружения

### Шаг 3: Открытие портов на роутере

1. **Зайдите в настройки роутера:**
   - Обычно: `192.168.1.1` или `192.168.0.1`
   - Логин/пароль обычно на наклейке роутера

2. **Найдите раздел "Port Forwarding" или "Виртуальные серверы":**
   - На разных роутерах может называться по-разному:
     - Port Forwarding
     - Port Mapping
     - Virtual Server
     - NAT Forwarding

3. **Создайте правило перенаправления портов:**
   - **Внешний порт:** `3000` (или любой другой, например `8080`)
   - **Внутренний IP:** ваш локальный IP (например: `192.168.1.100`)
   - **Внутренний порт:** `3000`
   - **Протокол:** TCP
   - **Описание:** Backend API

4. **Сохраните настройки**

### Шаг 4: Настройка Firewall на Mac

1. **Откройте Системные настройки → Защита и безопасность → Брандмауэр**

2. **Если брандмауэр включен:**
   - Нажмите "Параметры..."
   - Добавьте Node.js в список разрешенных приложений
   - Или временно отключите брандмауэр для тестирования

3. **Через терминал (альтернатива):**
   ```bash
   # Разрешить входящие соединения на порт 3000
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/local/bin/node
   
   # Или через pfctl (более продвинутый способ)
   sudo pfctl -f /etc/pf.conf
   ```

### Шаг 5: Запуск Backend

1. **Перейдите в папку backend:**
   ```bash
   cd backend
   ```

2. **Установите зависимости (если еще не установлены):**
   ```bash
   npm install
   ```

3. **Запустите сервер:**
   ```bash
   npm start
   # или для разработки с автоперезагрузкой:
   npm run dev
   ```

4. **Проверьте, что сервер запущен:**
   - Должно быть сообщение:
   ```
   ╔══════════════════════════════════════════════════════╗
   ║        🚀 ПРОКСИ СЕРВЕР КУРСОВ ВАЛЮТ                ║
   ╠══════════════════════════════════════════════════════╣
   ║  📡 Статус:    Запущен                               ║
   ║  🌐 Адрес:     http://0.0.0.0:3000                  ║
   ```

### Шаг 6: Проверка доступности

1. **Локально (на MacBook):**
   ```bash
   curl http://localhost:3000/api/health
   ```

2. **Извне (с другого устройства или через браузер):**
   ```
   http://ВАШ_СТАТИЧЕСКИЙ_IP:3000/api/health
   ```
   - Должен вернуться JSON с информацией о сервисе

3. **Если не работает:**
   - Проверьте, что порт открыт на роутере
   - Проверьте firewall на Mac
   - Убедитесь, что сервер запущен на `0.0.0.0`, а не `localhost`

### Шаг 7: Настройка Frontend

1. **Обновите `frontend/.env.local`:**
   ```env
   VITE_API_URL=http://ВАШ_СТАТИЧЕСКИЙ_IP:3000
   ```
   Например:
   ```env
   VITE_API_URL=http://123.45.67.89:3000
   ```

2. **Перезапустите frontend dev сервер:**
   ```bash
   cd frontend
   npm run dev
   ```

3. **Для production build:**
   ```bash
   npm run build
   ```
   - После билда переменная `VITE_API_URL` будет встроена в код

### Шаг 8: Настройка Telegram Web App

1. **Если у вас есть домен, настройте DNS:**
   - Создайте A-запись, указывающую на ваш статический IP
   - Например: `api.yourdomain.com` → `123.45.67.89`

2. **Обновите Web App URL в @BotFather:**
   - Вариант 1 (с доменом):
     ```
     https://yourdomain.com
     ```
   - Вариант 2 (без домена, только IP):
     ```
     http://123.45.67.89:5173
     ```
     ⚠️ **Внимание:** Telegram требует HTTPS для Web Apps в production!
     - Для тестирования можно использовать HTTP
     - Для production нужен HTTPS (используйте ngrok или настройте SSL)

---

## 🔒 Безопасность

### ⚠️ Важные меры безопасности:

1. **Используйте HTTPS в production:**
   - Установите SSL сертификат (Let's Encrypt бесплатный)
   - Или используйте reverse proxy (nginx) с SSL

2. **Ограничьте доступ:**
   - Настройте firewall на роутере
   - Разрешите доступ только с определенных IP (если возможно)
   - Используйте VPN для доступа к backend

3. **Не показывайте Bot Token:**
   - Храните в `.env` файле
   - Не коммитьте в Git
   - Используйте сильные пароли

4. **Регулярно обновляйте:**
   - Node.js
   - Зависимости (`npm update`)
   - macOS

---

## 🛠️ Использование ngrok для HTTPS (альтернатива)

Если у вас нет SSL сертификата, используйте ngrok:

1. **Установите ngrok:**
   ```bash
   brew install ngrok
   # или скачайте с ngrok.com
   ```

2. **Запустите туннель для frontend:**
   ```bash
   ngrok http 5173
   ```

3. **Запустите туннель для backend:**
   ```bash
   ngrok http 3000
   ```

4. **Используйте ngrok URLs:**
   - Frontend: `https://abc123.ngrok.io`
   - Backend: `https://xyz789.ngrok.io`
   - Обновите `VITE_API_URL` на ngrok URL backend

5. **Обновите Web App URL в @BotFather:**
   ```
   https://abc123.ngrok.io
   ```

⚠️ **Ограничение:** Бесплатный ngrok меняет URL при каждом перезапуске. Для production нужен платный план или свой домен.

---

## 🐛 Устранение проблем

### Проблема: Backend недоступен извне

**Решение:**
1. Проверьте, что сервер запущен на `0.0.0.0`, а не `localhost`
2. Проверьте настройки роутера (port forwarding)
3. Проверьте firewall на Mac
4. Проверьте, что порт не занят другим приложением:
   ```bash
   lsof -i :3000
   ```

### Проблема: CORS ошибки

**Решение:**
- Backend уже настроен на разрешение CORS для всех доменов
- Если проблемы остаются, проверьте заголовки в браузере

### Проблема: Telegram Web App не открывается

**Решение:**
- Убедитесь, что используется HTTPS (или ngrok)
- Проверьте, что URL доступен из интернета
- Проверьте, что скрипт Telegram SDK загружается

### Проблема: Порт уже занят

**Решение:**
```bash
# Найти процесс, использующий порт
lsof -i :3000

# Убить процесс
kill -9 <PID>

# Или используйте другой порт в .env
PORT=3001
```

---

## 📝 Чеклист настройки

- [ ] Статический IP известен
- [ ] Backend настроен на `HOST=0.0.0.0`
- [ ] Порт открыт на роутере (port forwarding)
- [ ] Firewall на Mac настроен
- [ ] Backend запущен и доступен локально
- [ ] Backend доступен извне (проверено через другой IP)
- [ ] Frontend настроен на правильный API URL
- [ ] Telegram Web App URL обновлен в @BotFather
- [ ] Протестирована отправка заявки
- [ ] Администратор получает сообщения в Telegram

---

## 🔄 Автозапуск при перезагрузке Mac

Чтобы backend запускался автоматически:

1. **Создайте LaunchAgent (рекомендуется):**
   ```bash
   # Создайте файл
   nano ~/Library/LaunchAgents/com.currencyexchange.backend.plist
   ```

2. **Добавьте содержимое:**
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
   <plist version="1.0">
   <dict>
     <key>Label</key>
     <string>com.currencyexchange.backend</string>
     <key>ProgramArguments</key>
     <array>
       <string>/usr/local/bin/node</string>
       <string>/Users/ваше_имя/Documents/Front-End/CriptoBot/backend/proxy.js</string>
     </array>
     <key>WorkingDirectory</key>
     <string>/Users/ваше_имя/Documents/Front-End/CriptoBot/backend</string>
     <key>RunAtLoad</key>
     <true/>
     <key>KeepAlive</key>
     <true/>
     <key>StandardOutPath</key>
     <string>/tmp/backend.log</string>
     <key>StandardErrorPath</key>
     <string>/tmp/backend.error.log</string>
     <key>EnvironmentVariables</key>
     <dict>
       <key>PATH</key>
       <string>/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
     </dict>
   </dict>
   </plist>
   ```

3. **Загрузите сервис:**
   ```bash
   launchctl load ~/Library/LaunchAgents/com.currencyexchange.backend.plist
   ```

4. **Проверьте статус:**
   ```bash
   launchctl list | grep currencyexchange
   ```

---

## 📚 Полезные команды

```bash
# Проверить, что порт открыт
nc -zv localhost 3000

# Проверить внешний доступ
curl http://ВАШ_IP:3000/api/health

# Посмотреть логи backend
tail -f /tmp/backend.log

# Остановить автозапуск
launchctl unload ~/Library/LaunchAgents/com.currencyexchange.backend.plist

# Перезапустить сервис
launchctl unload ~/Library/LaunchAgents/com.currencyexchange.backend.plist
launchctl load ~/Library/LaunchAgents/com.currencyexchange.backend.plist
```

---

## 🎯 Готово!

Теперь ваш backend доступен по адресу:
```
http://ВАШ_СТАТИЧЕСКИЙ_IP:3000
```

Или если настроен домен:
```
https://api.yourdomain.com
```

