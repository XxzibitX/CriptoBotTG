name: Deploy to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /var/www/currency-exchange/CriptoBotTG
            
            echo "⬇️ Загружаем обновления из Git..."
            git fetch origin
            git reset --hard origin/main
            
            echo "🔍 Проверяем целостность package.json..."
            cd backend
            if ! node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8'))" 2>/dev/null; then
              echo "❌ Ошибка в package.json! Восстанавливаем..."
              git checkout HEAD -- package.json
            fi
            
            echo "🔄 Обновляем бекенд..."
            npm install --production
            pm2 restart currency-backend || pm2 start proxy.js --name "currency-backend"
            
            cd ../frontend
            echo "🔄 Собираем фронтенд..."
            npm ci
            npm run build
            
            echo "📁 Копируем файлы фронтенда..."
            mkdir -p ../public
            rm -rf ../public/*
            cp -r dist/* ../public/
            
            cd ../telegram-bot-telegraf
            echo "🤖 Обновляем Telegram бота..."
            
            if [ -f "package.json" ]; then
              echo "📦 Устанавливаем зависимости бота..."
              npm install --production
              
              if pm2 list | grep -q "vertex-bot-telegraf"; then
                pm2 restart vertex-bot-telegraf
              else
                if [ ! -f ".env" ]; then
                  echo "⚠️ ВНИМАНИЕ: .env не найден. Бот не запустится без токена!"
                fi
                pm2 start bot.js --name "vertex-bot-telegraf"
                pm2 save
              fi
            else
              echo "⚠️ Папка бота пуста. Пропускаем."
            fi
            
            echo "✅ Деплой завершен!"