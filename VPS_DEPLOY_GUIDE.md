# 🚀 Полное руководство по деплою на VPS для начинающих

## 📋 Что у вас есть

- **Backend**: Node.js сервер (Express) на порту 3000
- **Frontend**: Vue.js приложение (собранные статические файлы в `dist/`)

---

## 🎯 План деплоя

1. Подготовка VPS
2. Установка необходимого ПО
3. Настройка Backend
4. Настройка Frontend
5. Настройка Nginx (веб-сервер)
6. Настройка SSL (HTTPS)
7. Автозапуск при перезагрузке

---

## 📦 Шаг 1: Подготовка VPS

### 1.1 Подключение к серверу

Если у вас еще нет VPS, можете использовать:
- **DigitalOcean** (от $4/месяц)
- **Linode** (от $5/месяц)
- **Hetzner** (от €4/месяц)
- **Vultr** (от $2.50/месяц)

После покупки VPS вы получите:
- IP адрес (например: `123.45.67.89`)
- Логин (обычно `root`)
- Пароль или SSH ключ

### 1.2 Подключение через SSH

**На Windows:**
- Используйте **PuTTY** или **Windows Terminal**
- Или установите Git Bash

**На Mac/Linux:**
- Используйте встроенный терминал

```bash
ssh root@ВАШ_IP_АДРЕС
# Например: ssh root@123.45.67.89
```

Введите пароль когда попросит.

---

## 🔧 Шаг 2: Установка необходимого ПО

### 2.1 Обновление системы

```bash
# Для Ubuntu/Debian
apt update && apt upgrade -y

# Для CentOS/RHEL
yum update -y
```

### 2.2 Установка Node.js

```bash
# Установка Node.js 20.x (LTS версия)
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Проверка установки
node --version  # Должно показать v20.x.x
npm --version   # Должно показать версию npm
```

### 2.3 Установка Nginx

```bash
apt install -y nginx

# Запуск и автозапуск
systemctl start nginx
systemctl enable nginx

# Проверка статуса
systemctl status nginx
```

### 2.4 Установка PM2 (менеджер процессов для Node.js)

```bash
npm install -g pm2

# Настройка автозапуска
pm2 startup
# Выполните команду, которую покажет PM2
```

### 2.5 Установка Certbot (для SSL сертификатов)

```bash
apt install -y certbot python3-certbot-nginx
```

---

## 📁 Шаг 3: Загрузка проекта на сервер

### 3.1 Создание структуры папок

```bash
# Создаем папку для проекта
mkdir -p /var/www/currency-exchange
cd /var/www/currency-exchange
```

### 3.2 Загрузка файлов (3 способа)

#### Способ 1: Через Git (рекомендуется)

```bash
# Установка Git
apt install -y git

# Клонирование репозитория (если проект в Git)
git clone https://github.com/ваш-username/ваш-репозиторий.git .

# Или если репозиторий приватный, используйте SSH ключ
```

#### Способ 2: Через SCP (с вашего компьютера)

**На вашем компьютере (Mac/Linux):**
```bash
cd /Users/a1111/Documents/Front-End/CriptoBotTG
scp -r backend root@ВАШ_IP:/var/www/currency-exchange/
scp -r frontend root@ВАШ_IP:/var/www/currency-exchange/
```

**На Windows (используйте WinSCP или FileZilla):**
- Подключитесь по SFTP
- Перетащите папки `backend` и `frontend`

#### Способ 3: Через zip архив

**На вашем компьютере:**
```bash
cd /Users/a1111/Documents/Front-End/CriptoBotTG
zip -r project.zip backend frontend
scp project.zip root@ВАШ_IP:/var/www/currency-exchange/
```

**На сервере:**
```bash
cd /var/www/currency-exchange
apt install -y unzip
unzip project.zip
rm project.zip
```

---

## ⚙️ Шаг 4: Настройка Backend

### 4.1 Установка зависимостей

```bash
cd /var/www/currency-exchange/backend
npm install --production
```

### 4.2 Создание файла с переменными окружения

```bash
nano .env
```

Вставьте следующее (замените на ваши значения):

```env
# Порт сервера
PORT=3000

# URL API для курсов валют
RAPIRA_API_URL=https://api.rapira.net/open/market/rates

# Путь к файлу с заявками
ORDERS_FILE=/var/www/currency-exchange/backend/data/orders.json

# Telegram Bot (опционально, но рекомендуется)
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id

# Режим работы
NODE_ENV=production
```

**Как получить Telegram Bot Token:**
1. Найдите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

**Как получить Chat ID:**
1. Найдите @userinfobot в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID

Сохраните файл: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

### 4.3 Создание папки для данных

```bash
mkdir -p /var/www/currency-exchange/backend/data
chmod 755 /var/www/currency-exchange/backend/data
```

### 4.4 Запуск Backend через PM2

```bash
cd /var/www/currency-exchange/backend
pm2 start proxy.js --name "currency-backend"
pm2 save
```

**Проверка:**
```bash
pm2 status
pm2 logs currency-backend
```

Backend должен быть доступен на `http://ВАШ_IP:3000`

---

## 🎨 Шаг 5: Настройка Frontend

### 5.1 Сборка Frontend (если еще не собрано)

```bash
cd /var/www/currency-exchange/frontend
npm install
npm run build
```

### 5.2 Обновление API URL в коде (если нужно)

Проверьте файл `frontend/src/components/MainVue.vue` - там должны быть URL для API.

Если они указывают на `localhost` или другой адрес, нужно обновить на ваш домен/IP.

**Найдите строки с:**
```javascript
const API_URL = '...'
const API_ORDERS_URL = '...'
const API_TELEGRAM_URL = '...'
```

**Измените на:**
```javascript
const API_URL = 'https://ваш-домен.com/api/rates'
const API_ORDERS_URL = 'https://ваш-домен.com/api/orders'
const API_TELEGRAM_URL = 'https://ваш-домен.com/api/telegram/send'
```

Затем пересоберите:
```bash
npm run build
```

### 5.3 Копирование собранных файлов

```bash
# Создаем папку для статических файлов
mkdir -p /var/www/currency-exchange/public

# Копируем собранные файлы
cp -r /var/www/currency-exchange/frontend/dist/* /var/www/currency-exchange/public/
```

---

## 🌐 Шаг 6: Настройка Nginx

### 6.1 Создание конфигурации Nginx

```bash
nano /etc/nginx/sites-available/currency-exchange
```

Вставьте следующую конфигурацию (замените `ваш-домен.com` на ваш домен или IP):

```nginx
# Проксирование API запросов к Backend
upstream backend {
    server 127.0.0.1:3000;
}

server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    # Логи
    access_log /var/log/nginx/currency-exchange-access.log;
    error_log /var/log/nginx/currency-exchange-error.log;

    # Максимальный размер загружаемых файлов
    client_max_body_size 10M;

    # API запросы - проксируем к Backend
    location /api/ {
        proxy_pass http://backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Статические файлы Frontend
    location / {
        root /var/www/currency-exchange/public;
        index index.html;
        try_files $uri $uri/ /index.html;
        
        # Кэширование статических файлов
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # Безопасность
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 6.2 Активация конфигурации

```bash
# Создаем символическую ссылку
ln -s /etc/nginx/sites-available/currency-exchange /etc/nginx/sites-enabled/

# Удаляем дефолтную конфигурацию (опционально)
rm /etc/nginx/sites-enabled/default

# Проверяем конфигурацию
nginx -t

# Перезагружаем Nginx
systemctl reload nginx
```

### 6.3 Настройка Firewall

```bash
# Установка UFW (если еще не установлен)
apt install -y ufw

# Разрешаем SSH (важно сделать первым!)
ufw allow 22/tcp

# Разрешаем HTTP и HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# Включаем firewall
ufw enable

# Проверка статуса
ufw status
```

---

## 🔒 Шаг 7: Настройка SSL (HTTPS)

### 7.1 Получение SSL сертификата

```bash
# Если у вас есть домен
certbot --nginx -d ваш-домен.com -d www.ваш-домен.com

# Следуйте инструкциям:
# - Введите email
# - Согласитесь с условиями
# - Выберите redirect HTTP на HTTPS
```

### 7.2 Автоматическое обновление сертификата

Certbot автоматически настроит обновление, но можно проверить:

```bash
certbot renew --dry-run
```

### 7.3 Если нет домена (только IP)

Если у вас только IP адрес, SSL сертификат получить нельзя. Но можно:
- Использовать самоподписанный сертификат (браузер будет показывать предупреждение)
- Или купить домен (от $1-2/год на Namecheap, GoDaddy и т.д.)

---

## ✅ Шаг 8: Проверка работы

### 8.1 Проверка Backend

```bash
# Проверка через curl
curl http://localhost:3000/api/health

# Должен вернуть JSON с информацией о сервере
```

### 8.2 Проверка Frontend

Откройте в браузере:
- `http://ВАШ_IP` или `https://ваш-домен.com`

### 8.3 Проверка API

```bash
curl https://ваш-домен.com/api/health
curl https://ваш-домен.com/api/rates
```

---

## 🔄 Шаг 9: Автозапуск и мониторинг

### 9.1 PM2 команды

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs currency-backend

# Перезапуск
pm2 restart currency-backend

# Остановка
pm2 stop currency-backend

# Удаление из автозапуска
pm2 delete currency-backend
```

### 9.2 Мониторинг ресурсов

```bash
# Мониторинг в реальном времени
pm2 monit

# Информация о процессе
pm2 info currency-backend
```

---

## 🛠️ Шаг 10: Обновление проекта

Когда нужно обновить код:

```bash
# 1. Остановите приложение
pm2 stop currency-backend

# 2. Обновите код (через Git или загрузите новые файлы)
cd /var/www/currency-exchange
git pull  # или загрузите файлы заново

# 3. Обновите зависимости (если нужно)
cd backend
npm install --production

# 4. Пересоберите Frontend (если нужно)
cd ../frontend
npm install
npm run build
cp -r dist/* ../public/

# 5. Запустите приложение
cd ../backend
pm2 restart currency-backend

# 6. Проверьте логи
pm2 logs currency-backend --lines 50
```

---

## 🐛 Решение проблем

### Проблема: Backend не запускается

```bash
# Проверьте логи
pm2 logs currency-backend

# Проверьте, занят ли порт 3000
netstat -tulpn | grep 3000

# Проверьте переменные окружения
cat /var/www/currency-exchange/backend/.env
```

### Проблема: Nginx показывает 502 Bad Gateway

```bash
# Проверьте, запущен ли Backend
pm2 status

# Проверьте логи Nginx
tail -f /var/log/nginx/currency-exchange-error.log

# Проверьте конфигурацию Nginx
nginx -t
```

### Проблема: Frontend не загружается

```bash
# Проверьте права доступа
ls -la /var/www/currency-exchange/public

# Установите правильные права
chown -R www-data:www-data /var/www/currency-exchange/public
chmod -R 755 /var/www/currency-exchange/public
```

### Проблема: API запросы не работают

1. Проверьте CORS настройки в `backend/proxy.js`
2. Проверьте, что URL в Frontend правильные
3. Проверьте логи Backend: `pm2 logs currency-backend`

---

## 📊 Полезные команды

```bash
# Просмотр использования диска
df -h

# Просмотр использования памяти
free -h

# Просмотр активных процессов
top

# Просмотр сетевых подключений
netstat -tulpn

# Просмотр логов системы
journalctl -xe

# Перезагрузка сервера
reboot
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Измените SSH порт** (опционально):
   ```bash
   nano /etc/ssh/sshd_config
   # Измените Port 22 на Port 2222 (или другой)
   systemctl restart sshd
   ```

2. **Отключите вход по паролю, используйте SSH ключи**:
   ```bash
   # На вашем компьютере
   ssh-keygen -t rsa -b 4096
   ssh-copy-id root@ВАШ_IP
   ```

3. **Регулярно обновляйте систему**:
   ```bash
   apt update && apt upgrade -y
   ```

4. **Настройте автоматические бэкапы** (рекомендуется)

---

## 📝 Чеклист перед запуском

- [ ] Node.js установлен и работает
- [ ] Nginx установлен и работает
- [ ] PM2 установлен и настроен автозапуск
- [ ] Backend запущен через PM2
- [ ] Frontend собран и скопирован в `/var/www/currency-exchange/public`
- [ ] Nginx конфигурация создана и активирована
- [ ] Firewall настроен
- [ ] SSL сертификат установлен (если есть домен)
- [ ] Переменные окружения настроены
- [ ] Тесты пройдены (API работает, Frontend открывается)

---

## 🎉 Готово!

Ваш проект должен быть доступен по адресу:
- `http://ВАШ_IP` (или `https://ваш-домен.com`)

Если что-то не работает, проверьте логи и следуйте разделу "Решение проблем".

Удачи! 🚀

